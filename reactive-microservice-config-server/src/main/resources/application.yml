server:
  port: 8888
  
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/krishnamurthypradeep/reactivems-configrepo.git
          default-label: master
          timeout: 5
          clone-on-start: true