eureka:
 client:
  registerWithEureka: true
  fetchRegistry: true
  serviceUrl:
   defaultZone: http://eureka-service:8070/eureka/ 

server:
  port: 9003
  shutdown: graceful
  tomcat:
    connection-timeout: 2s
    keep-alive-timeout: 15s
    threads:
      max: 50
      min-spare: 5

spring:
  application:
    name: order-service
  lifecycle:
    timeout-per-shutdown-phase: 15s
  
  
    
  datasource:
    username: user
    password: password
    url: jdbc:postgresql://myapp-postgre:5432/mydb_catalog
    hikari:
      connection-timeout: 2000 #ms
      maximum-pool-size: 5

management:
  endpoints:
    web:
      exposure:
        include: refresh
